# Настройка Gemini API

## Быстрый старт

1. Создайте файл `.env` в корне проекта `MaadicareAI`:
```env
GEMINI_API_KEY=AIzaSyCTUVcmdYeuD3PoJr_XO1sERE4qnY82fTk
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-1.5-flash
PORT=3001
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите сервер:
```bash
npm run dev
```

## Что изменилось

- ✅ Вместо OpenAI теперь используется **Google Gemini API**
- ✅ Модель по умолчанию: `gemini-1.5-flash`
- ✅ API ключ передается через query параметр `?key=...`
- ✅ System prompts конвертируются в `systemInstruction`

## API Endpoints

Все endpoints остались без изменений:
- `POST /ai/message` - отправка сообщения
- `POST /ai/finalize` - завершение чата

## Проверка работы

После запуска сервера вы должны увидеть:
```
╔═══════════════════════════════════════════════╗
║   🏥 AI Medical Adapter - MVP                ║
║                                               ║
║   Server:  http://localhost:3001          ║
║   Model:   gemini-1.5-flash                    ║
║   Status:  ✅ Ready                           ║
╚═══════════════════════════════════════════════╝
```

Если видите предупреждение `⚠️  WARNING: GEMINI_API_KEY is not set!`, проверьте файл `.env`.

